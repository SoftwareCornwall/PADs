#ifndef EVENTNOTIFIER_H
#define EVENTNOTIFIER_H

#include <string>

class EventNotifier
{
    public:
        EventNotifier(std::string serverAddress, std::string boxID, std::string eventType);
    protected:
    private:
};

#endif // EVENTNOTIFIER_H
